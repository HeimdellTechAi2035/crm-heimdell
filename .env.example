# See apps/api/.env.example and apps/web/.env.example for specific configs

# ── SMTP (direct outbound email) ─────────────────────────
SMTP_HOST=smtp.livemail.co.uk
SMTP_PORT=465
SMTP_SECURE=true
SMTP_DEFAULT_FROM=andrew@remoteability.org

# ── Encryption (AES-256-GCM for sender passwords) ────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
APP_ENCRYPTION_KEY=
