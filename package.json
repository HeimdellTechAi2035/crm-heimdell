{
  "name": "heimdell-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Heimdell CRM - Netlify + Neon Postgres",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd apps/web && npm run build",
    "install:all": "cd apps/web && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
